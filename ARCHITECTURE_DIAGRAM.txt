```
┌──────────────────────────────────────────────────────────────────────────────┐
│                     SequoAlpha EC2 Deployment Architecture                    │
└──────────────────────────────────────────────────────────────────────────────┘

                                  ☁️  Internet
                                       │
                                       ▼
                            ┌──────────────────────┐
                            │   AWS Security Group │
                            │  Ports: 80, 443, 22  │
                            └──────────┬───────────┘
                                       │
                                       ▼
        ╔═══════════════════════════════════════════════════════════╗
        ║                    EC2 Instance (Ubuntu)                   ║
        ║                                                            ║
        ║  ┌────────────────────────────────────────────────────┐  ║
        ║  │               Nginx (Port 80/443)                  │  ║
        ║  │                                                     │  ║
        ║  │  ┌──────────────┐         ┌──────────────────┐   │  ║
        ║  │  │   Frontend   │         │   Backend API    │   │  ║
        ║  │  │  Static Files│         │  Reverse Proxy   │   │  ║
        ║  │  │      /       │         │      /api        │   │  ║
        ║  │  └──────────────┘         └────────┬─────────┘   │  ║
        ║  └──────────────────────────────────────┼────────────┘  ║
        ║                                          │                ║
        ║  ┌──────────────────────────────────────▼────────────┐  ║
        ║  │          Gunicorn (Port 8000)                     │  ║
        ║  │              ┌─────────────────┐                  │  ║
        ║  │              │   Flask App     │                  │  ║
        ║  │              │    (main.py)    │                  │  ║
        ║  │              └────────┬────────┘                  │  ║
        ║  │                       │                           │  ║
        ║  │         ┌─────────────┴─────────────┐            │  ║
        ║  │         ▼                           ▼            │  ║
        ║  └─────────┼───────────────────────────┼────────────┘  ║
        ║            │                           │                ║
        ║  ┌─────────▼──────────┐    ┌──────────▼───────────┐  ║
        ║  │    PostgreSQL      │    │    Local Uploads     │  ║
        ║  │   (Port 5432)      │    │   /backend/uploads   │  ║
        ║  │                    │    │                      │  ║
        ║  │ • User accounts    │    │ • Temporary storage  │  ║
        ║  │ • Document metadata│    │ • Falls back if S3   │  ║
        ║  │ • Session data     │    │   unavailable        │  ║
        ║  └────────────────────┘    └──────────────────────┘  ║
        ║                                                        ║
        ║  ┌────────────────────────────────────────────────┐  ║
        ║  │           Systemd Service Manager              │  ║
        ║  │                                                 │  ║
        ║  │  • Auto-starts on boot                         │  ║
        ║  │  • Restarts on failure                         │  ║
        ║  │  • Manages logs                                │  ║
        ║  └────────────────────────────────────────────────┘  ║
        ╚════════════════════════════════════════════════════════╝
                                       │
                                       ▼
                            ┌──────────────────────┐
                            │      AWS S3          │
                            │  Document Storage    │
                            │                      │
                            │ • Persistent files   │
                            │ • PDF documents      │
                            │ • Presigned URLs     │
                            └──────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                            Request Flow Examples                              │
└──────────────────────────────────────────────────────────────────────────────┘

1. Frontend Page Request:
   User → http://your-ec2-ip/ 
        → Nginx serves /home/ubuntu/sequoalpha/index.html
        → Browser receives HTML/CSS/JS

2. API Request (Login):
   User → http://your-ec2-ip/api/login
        → Nginx → http://127.0.0.1:8000/login
        → Gunicorn → Flask → PostgreSQL
        → Returns JWT token

3. File Upload:
   User → http://your-ec2-ip/api/admin/documents/upload
        → Nginx → Backend
        → Saves to /backend/uploads/
        → Uploads to S3
        → Saves metadata to PostgreSQL
        → Returns success

4. File Download:
   User → http://your-ec2-ip/api/documents/123/download
        → Backend checks S3
        → Generates presigned URL
        → Returns redirect to S3 URL
        → User downloads directly from S3


┌──────────────────────────────────────────────────────────────────────────────┐
│                         Service Management Flow                               │
└──────────────────────────────────────────────────────────────────────────────┘

Start:
  ./manage.sh start
    ↓
  systemctl start sequoalpha
    ↓
  Systemd executes: /home/ubuntu/sequoalpha/backend/venv/bin/gunicorn
    ↓
  Gunicorn starts 3 worker processes
    ↓
  Each worker loads Flask app (main.py)
    ↓
  App connects to PostgreSQL and S3
    ↓
  Service ready on port 8000

Restart:
  ./manage.sh restart
    ↓
  systemctl restart sequoalpha nginx
    ↓
  Graceful shutdown → New processes started
    ↓
  Zero downtime

Update:
  ./update.sh
    ↓
  git pull → pip install → restart services
    ↓
  Updated code deployed


┌──────────────────────────────────────────────────────────────────────────────┐
│                          File Locations on EC2                                │
└──────────────────────────────────────────────────────────────────────────────┘

/home/ubuntu/sequoalpha/
├── index.html                          ← Served by Nginx
├── frontend/                           ← Served by Nginx
├── css/                                ← Served by Nginx
├── images/                             ← Served by Nginx
├── backend/
│   ├── venv/                           ← Python virtual env
│   ├── .env                            ← ⚠️ YOU MUST CREATE THIS
│   ├── main.py                         ← Flask app
│   ├── uploads/                        ← Local file storage
│   └── ...
└── manage.sh                           ← Your control script

/etc/nginx/sites-available/sequoalpha   ← Nginx config
/etc/systemd/system/sequoalpha.service  ← Systemd config
/var/log/sequoalpha/                    ← Application logs
/var/log/nginx/                         ← Nginx logs


┌──────────────────────────────────────────────────────────────────────────────┐
│                          Monitoring & Logs                                    │
└──────────────────────────────────────────────────────────────────────────────┘

View Live Logs:
  ./manage.sh logs
  └─→ sudo journalctl -u sequoalpha -f

Check Status:
  ./manage.sh status
  └─→ Shows if services are running, uptime, memory usage

View Errors Only:
  ./manage.sh errors
  └─→ Last 50 error messages

Test Endpoints:
  ./manage.sh test
  └─→ Checks if backend and nginx respond

Nginx Logs:
  sudo tail -f /var/log/nginx/sequoalpha_access.log
  sudo tail -f /var/log/nginx/sequoalpha_error.log


┌──────────────────────────────────────────────────────────────────────────────┐
│                     Security Layers                                           │
└──────────────────────────────────────────────────────────────────────────────┘

Layer 1: AWS Security Group
  • Only allows ports 22, 80, 443
  • SSH restricted to your IP
  • Blocks all other ports

Layer 2: UFW Firewall (on EC2)
  • Allows OpenSSH
  • Allows Nginx Full (80, 443)
  • Denies everything else

Layer 3: Nginx
  • Only proxies specific endpoints
  • Denies access to /backend/ files
  • Adds security headers

Layer 4: Application (Flask)
  • JWT authentication
  • Password hashing (bcrypt)
  • Input validation
  • Admin-only endpoints

Layer 5: Database
  • Localhost only (not exposed)
  • Strong password
  • User-specific permissions


┌──────────────────────────────────────────────────────────────────────────────┐
│                        Deployment Workflow                                    │
└──────────────────────────────────────────────────────────────────────────────┘

Day 1: Initial Setup
  1. Launch EC2 → 2. Clone repo → 3. Run deploy_ec2.sh
  4. Edit .env → 5. Edit nginx.conf → 6. Restart services
  7. Test in browser → 8. Setup SSL → 9. Configure domain

Day 2+: Updates
  1. Make changes locally → 2. Push to GitHub
  3. SSH to EC2 → 4. Run ./update.sh → 5. Test

Daily: Monitoring
  1. ./manage.sh status (check health)
  2. ./manage.sh logs (watch activity)
  3. ./manage.sh backup (weekly backups)


┌──────────────────────────────────────────────────────────────────────────────┐
│                           Port Reference                                      │
└──────────────────────────────────────────────────────────────────────────────┘

Port 22    → SSH (restricted to your IP)
Port 80    → HTTP (Nginx) → All users
Port 443   → HTTPS (Nginx, after SSL setup) → All users
Port 5432  → PostgreSQL → Localhost only (not exposed)
Port 8000  → Gunicorn/Flask → Localhost only (proxied by Nginx)


Legend:
  ║ ═ ╔ ╗ ╚ ╝   Box borders
  ▼ ▲ ► ◄       Flow direction
  → ←           Process flow
  ☁️            Cloud/Internet
  ⚠️            Attention needed
  ✅            Success/Complete
  📁            Directory
```
